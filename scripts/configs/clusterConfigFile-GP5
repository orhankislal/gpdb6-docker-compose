ARRAY_NAME="GPDB Production Cluster"

MASTER_HOSTNAME=cdw
MASTER_DIRECTORY=/data/qddir
MASTER_PORT=5000

SEG_PREFIX=gpseg
PORT_BASE=5002
declare -a DATA_DIRECTORY=(/data/primary)

MIRROR_PORT_BASE=5502
declare -a MIRROR_DATA_DIRECTORY=(/data/mirror)

TRUSTED_SHELL=ssh
ENCODING=UNICODE
DEFAULT_QD_MAX_CONNECT=150
QE_CONNECT_FACTOR=5

MIRROR_REPLICATION_PORT_BASE=5700
REPLICATION_PORT_BASE=100
